md-sidenav.md-sidenav-left(md-component-id='left', md-whiteframe='4')
	//- md-toolbar.md-theme-indigo
	//- 	h1.md-toolbar-tools Disabled Backdrop
	//- md-content(layout-margin='')
	//- 	p
	//- 		| This sidenav is not showing any backdrop, where users can click on it, to close the sidenav.
	//- 	md-button.md-accent(ng-click='toggleLeft()')
	//- 		| Close this Sidenav
	md-list(flex='')
		md-list-item(ng-repeat='server in main.servers | filter: {Alias:main.searchAlias}')
			md-button(href='/server/{{server.escaped}}/{{server.Alias}}/{{server.Notes}}',ng-hide='((server.Description=="none") || !server.Alias)').sidenav-close
				md-icon.material-icons(ng-class="{'green' : (server.hbMin_ago<30 && server.hbMin_ago!==null && server.Min_ago===null) , 'red' : (server.hbMin_ago>30 || server.Min_ago!==null)}") computer
				| {{server.Alias}} - {{server.Description}}
		//- </ul>
